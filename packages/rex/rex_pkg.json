{
  "$schema": "../../schemas/rex_pkg.json",
  "publish": {
    "actions": [
      {
        "run": "deno bundle mod.ts index.js",
        "performFor": ["npm"]
      },
      {
        "name": "npm test",
        "run": "node index.js",
        "performFor": ["npm"]
      }
    ]
  }
}
